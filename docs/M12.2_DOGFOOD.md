# M12.2 Smart Router dogfood

Date: 2026-08-14

This is local evidence for M12.2. It is not a public release, an acceptance
claim, or proof of live provider inference.

## Scope

- Pi `0.84.1`, Node.js `v22.23.0`, disposable injected configuration roots.
- No live Pi configuration, provider account, credential, tag, push, npm
  publication, or GitHub release was modified.
- Live Primary/Fallback triage was not called. The current checkout had no
  securely available 9Router credential or registered triage route, so no
  credential was copied, printed, or manufactured for this check.

## Offline and fake evidence

| Case | Result |
|---|---|
| Clear explanation/question | Local `NORMAL`; no recommendation banner; no AI call |
| Clear multi-stage implementation request | Local `SUGGEST_MISSION`; recommendation banner; `Run as Mission` persisted one canonical Mission with the exact original goal |
| Run Normally | One-shot choice continued the exact original input without creating a Mission |
| Cancel | Original prompt was restored to the editor |
| Explicit `@orchestrator <goal>` | M12.1 parser bypassed Smart Routing and preserved canonical Mission behavior, including busy/store-failure requeue coverage |
| Ambiguous primary/fallback/degradation | Deterministic tests covered strict response validation, capability-only fallback, disagreement without fallback, timeout/auth/quota/transport degradation, and user-choice preservation |
| Routing settings | Existing twelve-section Control Center retained; Smart Routing settings appear inside Routing & Fallback with stale route visibility and versioned sidecar persistence |
| Telemetry | Routing and analyst persistence tests reject raw prompt/transcript/tool/secret-like values and retain bounded metadata only |

## Isolated Pi TUI

Using `pi --offline --no-extensions -e dist/host/pi-extension.js` with
disposable `PI_MULTI_ORCH_CONFIG_ROOT` and `PI_CODING_AGENT_DIR` roots:

- `Explain what a TypeScript interface is` reached the editor with no Smart
  Routing banner and was exited without a provider call.
- `Fix the bug in src/auth.ts and add tests, then verify independently` showed
  `Orchestrator recommended`; selecting the default `Run as Mission` created
  exactly one draft Mission whose title and goal matched the original text.
- The generated MissionStore was queried directly after the run; no session
  or live agent state was used.

## Open gates

Live English/Persian ambiguous triage, controlled Primary failure with a real
Fallback, and both-route-unavailable inference remain open external gates.
M12.3 Routing Memory and the M12 Final Gate remain not started. M10 remains the
latest accepted milestone.

## Final local release evidence

- Candidate: `0.1.0-rc.11`, commit `63929b8a63f475c04c9e61923e4e3b00d48e2f6e`.
- Exact-Git detached verifier: `190/190` tests, `20/20` integrity attacks,
  Pi `0.84.1` install/rollback PASS.
- Artifact SHA-256:
  `628df62c7aec701a2cced8f33fa8627414cf9032e7fe02a5a697ad741ada3443`.
- Review-bundle root SHA-256:
  `2383c420df259843eadab5902f9fb650151af7e80378ab1e51b87c588d56f516`.
